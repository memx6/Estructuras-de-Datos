#include <iostream>
#include "Arrays.h"

int sumatoria(int arr[], int len){
//Dada una lista de enteros devuelve la suma de todos sus elementos.
    int r = 0
    for( )
}
void mapSucesor(int arr[], int len){
//Dada una lista de enteros, devuelve la lista de los sucesores de cada entero.

}
bool todoVerdad (int arr[], int len){
//Dada una lista de booleanos devuelve True si todos sus elementos son True.

}
bool algunaVerdad(int arr[], int len){
//Dada una lista de booleanos devuelve True si alguno de sus elementos es True.

}
bool pertenece (int arr[], int len, int e){
//Dados un elemento e y una lista xs devuelve True si existe un elemento en xs que sea igual a e.

}
int apariciones (int arr[], int len, int e){
//Dados un elemento e y una lista xs cuenta la cantidad de apariciones de e en xs.

}
int* filtrarMenoresA (int arr[], int len, int e){
//Dados un número n y una lista xs, devuelve todos los elementos de xs que son menores a n.

}
int* filtrarElemento (int arr[], int len, int e){
//Dados un elemento y una lista filtra (elimina) todas las ocurrencias de ese elemento en la lista.

}